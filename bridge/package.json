{
  "name": "form-filler-bridge",
  "version": "1.3.0",
  "description": "Local bridge that uses Ollama to answer scanned form fields from a validated candidate profile.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/ollama.js && node --check lib/answers.js && node --check lib/profile.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
